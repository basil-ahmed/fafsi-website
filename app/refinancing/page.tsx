export const metadata = {
    title: "Refinancing",
    description: "Page description",
  };

import Refinancing from "@/components/refinancing";

export default function RefinancingPage() {
  return <Refinancing />;
}
